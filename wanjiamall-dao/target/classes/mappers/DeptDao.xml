<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nf.wanjiamall.dao.GoodsDao">

<!-- 上架商品  -->

    <insert id="insert">
        insert into wanjia_goods(goods_sn, name, category_id,
         brand_id, gallery, keywords, brief,
         is_on_sale, is_new, is_hot, unit,
         counter_price, retail_price, detail)
         values (#{goodsEntity.goodsSn},#{goodsEntity.name},#{goodsEntity.categoryId}
         ,#{goodsEntity.brandId},#{goodsEntity.gallery},#{goodsEntity.keywords}
         ,#{goodsEntity.brief},#{goodsEntity.isOnSale},#{goodsEntity.isNew},#{goodsEntity.isHot}
         ,{goodsEntity.unit},{goodsEntity.counterPrice},#{goodsEntity.retailPrice}
         ,{goodsEntity.detail})
    </insert>

</mapper>