<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.nf.wanjiamall.dao.CategoryDao">
    <select id="getCateAll" resultType="com.nf.wanjiamall.entity.CategoryEntity">
        select id, name, category_desc, pid, icon_url, pic_url, level, update_time,
         add_time, delted from wanjiamall_category where delted=0 order by id
    </select>

    <insert id="insertByLevelFirst">
        insert into wanjiamall_category(name, category_desc, pid,icon_url, pic_url,
         level, update_time, add_time, delted) values (#{name},#{categoryDesc},#{pid},
         #{iconUrl},#{picUrl},#{level},#{updateTime},#{addTime},#{delted})
    </insert>

    <select id="getFirstCate" resultType="com.nf.wanjiamall.entity.CategoryEntity">
        select id, name, category_desc, pid, icon_url, pic_url, level, add_time,
        update_time, delted from wanjiamall_category where delted=0 and level=1
    </select>

    <select id="getSecondCate" resultType="com.nf.wanjiamall.entity.CategoryEntity">
        select id, name, category_desc, pid, icon_url, pic_url, level, add_time,
        update_time, delted from wanjiamall_category where delted=0 and level=2 and pid=#{pid}
    </select>

    <update id="updateById">
        update wanjiamall_category set name=#{name},category_desc=#{categoryDesc},
        pid=#{pid},icon_url=#{iconUrl},pic_url=#{picUrl},level=#{level},
        update_time=#{updateTime} where delted=0 and id=#{id}
    </update>

</mapper>
